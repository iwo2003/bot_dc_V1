# Tło powitań / pożegnań

Umieść tutaj plik PNG/JPG wskazany w `welcome.json`:

```json
"backgroundImage": "assets/welcome/background.png"
```

Jeśli plik nie istnieje, bot użyje tylko koloru tła (`backgroundColor`).
